Rows with some real evidence should move from MISSING to PARTIAL instead of being dropped too harshly.
Diagram items with good text alignment should stop failing only because the multimodal caption text is weak.
If you still see wrong results after this run, send me one failing tag from the JSON output along with its matched_requirement_items, missing_requirement_items, and diagram_reference_alignment values, and I can tune that case directly.
